@{
    ViewData["Title"] = "Full Names";
}

@model List<string>

<h1>@ViewData["Title"]</h1>

@if (Model.Count == 0) {
    <p>No data to show.</p>
} else {
    <ul>
        @foreach (string name in Model) {
            <li>@name</li>
        }
    </ul>
}